# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar toggle dark/light mode na sidebar com persistência em arquivo local, usando `base = "dark"` nativo do Streamlit + injeção de CSS para modo claro.

**Architecture:** `config.toml` define `base = "dark"` como padrão nativo. Uma função `_injetar_tema()` em `ui/utils.py` injeta CSS mínimo para dark (corrige HTML customizado) ou CSS abrangente para light (reverte componentes nativos). A preferência é salva em `.streamlit/ui_prefs.json` e carregada no `session_state` na primeira visita de cada sessão.

**Tech Stack:** Python 3.12, Streamlit, pytest, pathlib, json (stdlib).

---

## File Map

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `.streamlit/config.toml` | Modificar | Trocar `base = "light"` → `base = "dark"` |
| `ui/utils.py` | Modificar | Adicionar `_ler_tema`, `_salvar_tema`, `_css_tema`, `_injetar_tema`, `_sidebar_tema`; atualizar `_badge_etiqueta` |
| `tests/test_ui_utils.py` | Criar | Testes para os helpers puros de tema |
| `ui/pages/acervo.py` | Modificar | Chamar `_injetar_tema()` e `_sidebar_tema()`; remover CSS inline dos botões; usar classe `.capa-placeholder` |
| `ui/pages/ficha.py` | Modificar | Chamar `_injetar_tema()` e `_sidebar_tema()`; usar classe `.capa-placeholder` |
| `ui/pages/estantes.py` | Modificar | Chamar `_injetar_tema()` e `_sidebar_tema()` |
| `ui/pages/leitura.py` | Modificar | Chamar `_injetar_tema()` e `_sidebar_tema()` |
| `ui/pages/sobre.py` | Modificar | Chamar `_injetar_tema()` e `_sidebar_tema()` |

---

## Task 1: Helpers de persistência + `_badge_etiqueta` sem cor inline

**Files:**
- Modify: `ui/utils.py`
- Create: `tests/test_ui_utils.py`

- [ ] **Step 1: Criar `tests/test_ui_utils.py` com os testes failing**

```python
# tests/test_ui_utils.py
from pathlib import Path
import json
import pytest


def test_ler_tema_default_quando_sem_arquivo(tmp_path, monkeypatch):
    """Sem arquivo de prefs, retorna 'escuro'."""
    import ui.utils as utils
    monkeypatch.setattr(utils, "_PREFS_PATH", tmp_path / "ui_prefs.json")
    assert utils._ler_tema() == "escuro"


def test_ler_tema_retorna_valor_salvo(tmp_path, monkeypatch):
    import ui.utils as utils
    path = tmp_path / "ui_prefs.json"
    path.write_text(json.dumps({"tema": "claro"}), encoding="utf-8")
    monkeypatch.setattr(utils, "_PREFS_PATH", path)
    assert utils._ler_tema() == "claro"


def test_ler_tema_json_invalido_retorna_default(tmp_path, monkeypatch):
    import ui.utils as utils
    path = tmp_path / "ui_prefs.json"
    path.write_text("NOT JSON", encoding="utf-8")
    monkeypatch.setattr(utils, "_PREFS_PATH", path)
    assert utils._ler_tema() == "escuro"


def test_salvar_tema_cria_arquivo(tmp_path, monkeypatch):
    import ui.utils as utils
    path = tmp_path / "ui_prefs.json"
    monkeypatch.setattr(utils, "_PREFS_PATH", path)
    utils._salvar_tema("claro")
    assert json.loads(path.read_text()) == {"tema": "claro"}


def test_salvar_tema_sobrescreve_valor_anterior(tmp_path, monkeypatch):
    import ui.utils as utils
    path = tmp_path / "ui_prefs.json"
    path.write_text(json.dumps({"tema": "claro"}), encoding="utf-8")
    monkeypatch.setattr(utils, "_PREFS_PATH", path)
    utils._salvar_tema("escuro")
    assert json.loads(path.read_text()) == {"tema": "escuro"}


def test_badge_etiqueta_usa_classe_css():
    """Badge deve usar class="badge-etiqueta" sem inline style de cor."""
    import ui.utils as utils
    html = utils._badge_etiqueta("doutorado")
    assert 'class="badge-etiqueta"' in html
    assert "background:#ede7f6" not in html
    assert "color:#6a1b9a" not in html
    assert "doutorado" in html
```

- [ ] **Step 2: Rodar os testes para confirmar que falham**

```bash
pytest tests/test_ui_utils.py -v
```

Esperado: 6 falhas com `AttributeError: module 'ui.utils' has no attribute '_PREFS_PATH'` e similares.

- [ ] **Step 3: Implementar os helpers em `ui/utils.py`**

Adicionar logo após os imports existentes (antes de `FONTE_CORES`):

```python
import json

_PREFS_PATH = Path(__file__).parent.parent / ".streamlit" / "ui_prefs.json"


def _ler_tema() -> str:
    try:
        data = json.loads(_PREFS_PATH.read_text(encoding="utf-8"))
        return data.get("tema", "escuro")
    except (FileNotFoundError, json.JSONDecodeError):
        return "escuro"


def _salvar_tema(tema: str) -> None:
    _PREFS_PATH.parent.mkdir(parents=True, exist_ok=True)
    _PREFS_PATH.write_text(json.dumps({"tema": tema}), encoding="utf-8")
```

Atualizar `_badge_etiqueta()` (atualmente em `ui/utils.py:153-157`):

```python
def _badge_etiqueta(etiqueta: str) -> str:
    return (
        f'<span class="badge-etiqueta" style="padding:2px 8px;'
        f'border-radius:12px;font-size:0.75rem;font-weight:500">{etiqueta}</span>'
    )
```

- [ ] **Step 4: Rodar os testes para confirmar que passam**

```bash
pytest tests/test_ui_utils.py -v
```

Esperado: 6 PASS.

- [ ] **Step 5: Rodar a suite completa para garantir nenhuma regressão**

```bash
pytest
```

Esperado: todos os testes existentes passam.

- [ ] **Step 6: Commit**

```bash
git add ui/utils.py tests/test_ui_utils.py
git commit -m "feat(tema): helpers de persistência e badge sem cor inline"
```

---

## Task 2: `_css_tema()` e `_injetar_tema()`

**Files:**
- Modify: `ui/utils.py`
- Modify: `tests/test_ui_utils.py`

- [ ] **Step 1: Adicionar testes para `_css_tema()` em `tests/test_ui_utils.py`**

```python
def test_css_tema_dark_contem_badge_escuro():
    import ui.utils as utils
    css = utils._css_tema(dark=True)
    assert ".badge-etiqueta" in css
    assert "#2d1b4e" in css   # fundo roxo escuro


def test_css_tema_dark_contem_capa_placeholder_escuro():
    import ui.utils as utils
    css = utils._css_tema(dark=True)
    assert ".capa-placeholder" in css
    assert "#2d2d2d" in css   # cinza escuro


def test_css_tema_dark_contem_botao_titulo_claro():
    import ui.utils as utils
    css = utils._css_tema(dark=True)
    assert "#fafafa" in css   # texto branco nos botões de título


def test_css_tema_light_contem_badge_claro():
    import ui.utils as utils
    css = utils._css_tema(dark=False)
    assert "#ede7f6" in css   # fundo roxo claro
    assert "#6a1b9a" in css   # texto roxo


def test_css_tema_light_contem_override_stapp():
    import ui.utils as utils
    css = utils._css_tema(dark=False)
    assert ".stApp" in css
    assert "#ffffff" in css


def test_css_tema_light_contem_sidebar():
    import ui.utils as utils
    css = utils._css_tema(dark=False)
    assert "stSidebar" in css
    assert "#f0f2f6" in css
```

- [ ] **Step 2: Rodar os novos testes para confirmar que falham**

```bash
pytest tests/test_ui_utils.py::test_css_tema_dark_contem_badge_escuro -v
```

Esperado: FAIL com `AttributeError: module 'ui.utils' has no attribute '_css_tema'`.

- [ ] **Step 3: Implementar `_css_tema()` e `_injetar_tema()` em `ui/utils.py`**

Adicionar após `_salvar_tema()`:

```python
def _css_tema(dark: bool) -> str:
    if dark:
        return """
.badge-etiqueta { background: #2d1b4e; color: #ce93d8; }
.capa-placeholder { background: #2d2d2d; }
div[data-testid="column"] button[kind="secondary"] {
    background: none !important; border: none !important; box-shadow: none !important;
    text-align: left !important; font-weight: 600 !important;
    padding: 2px 0 !important; cursor: pointer !important;
    line-height: 1.4 !important; white-space: normal !important;
    color: #fafafa !important; width: 100% !important;
}
div[data-testid="column"] button[kind="secondary"]:hover {
    color: #90caf9 !important; background: none !important; box-shadow: none !important;
}
"""
    return """
.badge-etiqueta { background: #ede7f6; color: #6a1b9a; }
.capa-placeholder { background: #eceff1; }
div[data-testid="column"] button[kind="secondary"] {
    background: none !important; border: none !important; box-shadow: none !important;
    text-align: left !important; font-weight: 600 !important;
    padding: 2px 0 !important; cursor: pointer !important;
    line-height: 1.4 !important; white-space: normal !important;
    color: rgb(49,51,63) !important; width: 100% !important;
}
div[data-testid="column"] button[kind="secondary"]:hover {
    color: #1565c0 !important; background: none !important; box-shadow: none !important;
}
.stApp { background-color: #ffffff !important; }
[data-testid="stAppViewContainer"],
[data-testid="stAppViewContainer"] > .main { background-color: #ffffff !important; }
section[data-testid="stSidebar"],
section[data-testid="stSidebar"] > div { background-color: #f0f2f6 !important; }
[data-testid="metric-container"] { background-color: #f0f2f6 !important; border: 1px solid #e6e9ef !important; }
[data-testid="stMetricValue"], [data-testid="stMetricLabel"] { color: #31333f !important; }
.stTextInput input { background-color: #ffffff !important; color: #31333f !important; }
[data-testid="stExpander"] { background-color: #f0f2f6 !important; }
"""


def _injetar_tema() -> None:
    dark = st.session_state.get("tema", "escuro") == "escuro"
    st.markdown(f"<style>{_css_tema(dark)}</style>", unsafe_allow_html=True)
```

- [ ] **Step 4: Rodar todos os testes**

```bash
pytest tests/test_ui_utils.py -v
```

Esperado: todos os 12 testes PASS.

- [ ] **Step 5: Commit**

```bash
git add ui/utils.py tests/test_ui_utils.py
git commit -m "feat(tema): _css_tema e _injetar_tema com CSS dark/light"
```

---

## Task 3: `config.toml` dark base

**Files:**
- Modify: `.streamlit/config.toml`

- [ ] **Step 1: Trocar `base` no config**

Conteúdo novo de `.streamlit/config.toml`:

```toml
[server]
headless = true

[theme]
base = "dark"
```

- [ ] **Step 2: Commit**

```bash
git add .streamlit/config.toml
git commit -m "feat(tema): base dark no config.toml"
```

---

## Task 4: `_sidebar_tema()`

**Files:**
- Modify: `ui/utils.py`

- [ ] **Step 1: Implementar `_sidebar_tema()` em `ui/utils.py`**

Adicionar após `_injetar_tema()`:

```python
def _sidebar_tema() -> None:
    if "tema" not in st.session_state:
        st.session_state["tema"] = _ler_tema()
    with st.sidebar:
        st.divider()
        claro = st.toggle(
            "☀️ Modo claro",
            value=st.session_state["tema"] == "claro",
            key="toggle_tema",
        )
        novo = "claro" if claro else "escuro"
        if novo != st.session_state["tema"]:
            st.session_state["tema"] = novo
            _salvar_tema(novo)
            st.rerun()
```

- [ ] **Step 2: Rodar suite para garantir nenhuma regressão**

```bash
pytest
```

Esperado: todos os testes passam.

- [ ] **Step 3: Commit**

```bash
git add ui/utils.py
git commit -m "feat(tema): _sidebar_tema com toggle e persistência"
```

---

## Task 5: Atualizar `acervo.py`

**Files:**
- Modify: `ui/pages/acervo.py`

- [ ] **Step 1: Adicionar `_injetar_tema` e `_sidebar_tema` aos imports**

Na linha do `from ui.utils import (...)` em `acervo.py`, adicionar `_injetar_tema` e `_sidebar_tema`:

```python
from ui.utils import (
    ESTILOS,
    _IDIOMA_NORM,
    _badge,
    _badge_capa,
    _badge_etiqueta,
    _carregar,
    _dialog_editar,
    _dialog_login,
    _estatisticas,
    _injetar_tema,
    _is_autenticado,
    _normalizar,
    _session_bar,
    _sidebar_tema,
)
```

- [ ] **Step 2: Chamar `_injetar_tema()` logo após os imports (antes de `st.title`)**

Adicionar logo antes de `st.title("📚 Minha Biblioteca")`:

```python
_injetar_tema()
```

- [ ] **Step 3: Remover o bloco de CSS inline dos botões de título**

Localizar e remover o bloco abaixo (linhas ~135-154 em `acervo.py`). Esse CSS agora vem de `_injetar_tema()`:

```python
    st.markdown("""<style>
div[data-testid="column"] button[kind="secondary"] {
    background: none !important;
    border: none !important;
    box-shadow: none !important;
    text-align: left !important;
    font-weight: 600 !important;
    padding: 2px 0 !important;
    cursor: pointer !important;
    line-height: 1.4 !important;
    white-space: normal !important;
    color: rgb(49,51,63) !important;
    width: 100% !important;
}
div[data-testid="column"] button[kind="secondary"]:hover {
    color: #1565c0 !important;
    background: none !important;
    box-shadow: none !important;
}
</style>""", unsafe_allow_html=True)
```

- [ ] **Step 4: Atualizar o placeholder de capa sem capa (usa classe)**

Localizar:
```python
                    st.markdown(
                        '<div style="height:260px;background:#eceff1;display:flex;'
                        'align-items:center;justify-content:center;font-size:3rem;'
                        'border-radius:4px;cursor:pointer">📖</div>',
                        unsafe_allow_html=True,
                    )
```

Substituir por:
```python
                    st.markdown(
                        '<div class="capa-placeholder" style="height:260px;display:flex;'
                        'align-items:center;justify-content:center;font-size:3rem;'
                        'border-radius:4px;cursor:pointer">📖</div>',
                        unsafe_allow_html=True,
                    )
```

- [ ] **Step 5: Chamar `_sidebar_tema()` no final da página (após `_session_bar()`)**

```python
_session_bar()
_sidebar_tema()
```

- [ ] **Step 6: Commit**

```bash
git add ui/pages/acervo.py
git commit -m "feat(tema): dark mode em acervo.py"
```

---

## Task 6: Atualizar `ficha.py`

**Files:**
- Modify: `ui/pages/ficha.py`

- [ ] **Step 1: Adicionar `_injetar_tema` e `_sidebar_tema` aos imports**

Na linha `from ui.utils import (...)`, adicionar os dois:

```python
from ui.utils import (
    _IDIOMA_NORM,
    _badge,
    _badge_capa,
    _badge_etiqueta,
    _carregar,
    _dialog_editar,
    _dialog_login,
    _injetar_tema,
    _is_autenticado,
    _session_bar,
    _sidebar_tema,
)
```

- [ ] **Step 2: Chamar `_injetar_tema()` logo após os imports, antes dos guards**

Adicionar logo antes de `isbn = st.session_state.get("isbn_ficha")`:

```python
_injetar_tema()
```

- [ ] **Step 3: Atualizar o placeholder de capa em `ficha.py`**

Localizar:
```python
        st.markdown(
            '<div style="height:280px;background:#eceff1;display:flex;'
            'align-items:center;justify-content:center;font-size:4rem;'
            'border-radius:8px">📖</div>',
            unsafe_allow_html=True,
        )
```

Substituir por:
```python
        st.markdown(
            '<div class="capa-placeholder" style="height:280px;display:flex;'
            'align-items:center;justify-content:center;font-size:4rem;'
            'border-radius:8px">📖</div>',
            unsafe_allow_html=True,
        )
```

- [ ] **Step 4: Chamar `_sidebar_tema()` no final (após `_session_bar()`)**

```python
_session_bar()
_sidebar_tema()
```

- [ ] **Step 5: Commit**

```bash
git add ui/pages/ficha.py
git commit -m "feat(tema): dark mode em ficha.py"
```

---

## Task 7: Atualizar páginas restantes (estantes, leitura, sobre)

**Files:**
- Modify: `ui/pages/estantes.py`
- Modify: `ui/pages/leitura.py`
- Modify: `ui/pages/sobre.py`

### `estantes.py`

- [ ] **Step 1: Adicionar `_injetar_tema` e `_sidebar_tema` ao import de `ui.utils`**

Localizar:
```python
from ui.utils import ESTILOS, _carregar, _carregar_config, _session_bar
```

Substituir por:
```python
from ui.utils import ESTILOS, _carregar, _carregar_config, _injetar_tema, _session_bar, _sidebar_tema
```

- [ ] **Step 2: Chamar `_injetar_tema()` antes do primeiro `st.` renderizável**

Adicionar logo antes de `if st.button("← Voltar ao acervo"):`:

```python
_injetar_tema()
```

- [ ] **Step 3: Chamar `_sidebar_tema()` no final da página (após `_session_bar()`)**

Localizar `_session_bar()` no final do arquivo e adicionar depois:

```python
_sidebar_tema()
```

### `leitura.py`

- [ ] **Step 4: Adicionar `_injetar_tema` e `_sidebar_tema` ao import de `ui.utils`**

Localizar:
```python
from ui.utils import _dialog_login, _is_autenticado, _session_bar
```

Substituir por:
```python
from ui.utils import _dialog_login, _injetar_tema, _is_autenticado, _session_bar, _sidebar_tema
```

- [ ] **Step 5: Chamar `_injetar_tema()` antes do primeiro `st.` renderizável**

Adicionar logo antes de `if st.button("← Voltar ao acervo"):`:

```python
_injetar_tema()
```

- [ ] **Step 6: Chamar `_sidebar_tema()` após `_session_bar()` no final da página**

```python
_session_bar()
_sidebar_tema()
```

### `sobre.py`

- [ ] **Step 7: Adicionar import de `ui.utils` em `sobre.py`**

Adicionar após os imports existentes:

```python
from ui.utils import _injetar_tema, _sidebar_tema
```

- [ ] **Step 8: Chamar `_injetar_tema()` antes do primeiro `st.` renderizável**

Adicionar logo antes de `if st.button("← Voltar ao acervo"):`:

```python
_injetar_tema()
```

- [ ] **Step 9: Chamar `_sidebar_tema()` no final da página**

Adicionar ao final do arquivo:

```python
_sidebar_tema()
```

- [ ] **Step 10: Rodar a suite completa**

```bash
pytest
```

Esperado: todos os testes passam.

- [ ] **Step 11: Commit**

```bash
git add ui/pages/estantes.py ui/pages/leitura.py ui/pages/sobre.py
git commit -m "feat(tema): dark mode nas páginas restantes"
```

---

## Verificação manual

Após todos os commits, iniciar o app e verificar:

```bash
streamlit run ui/app.py
```

1. App abre em dark mode por padrão
2. Toggle "☀️ Modo claro" aparece na sidebar de todas as páginas
3. Ao ativar o toggle, o app alterna para light mode imediatamente
4. Fechar o browser e reabrir o app — tema continua o mesmo (persistência)
5. Badges de etiqueta ficam legíveis em ambos os temas
6. Placeholder de capa usa cinza escuro em dark, cinza claro em light
7. Botões de título dos cards ficam brancos em dark, escuros em light
