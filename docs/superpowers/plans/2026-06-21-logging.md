# Logging Estruturado — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar o módulo `logging` do Python ao projeto, gravando um arquivo de log diário em `data/logs/` com trace completo e enriquecendo o terminal com um resumo compacto de fonte/capa/campos após cada ISBN processado.

**Architecture:** Um logger raiz `catalog` configurado em `scripts/main.py` com dois handlers — `StreamHandler(WARNING)` para o terminal (só erros graves) e `FileHandler(DEBUG)` para `data/logs/YYYY-MM-DD.log`. Cada módulo `catalog.*` adiciona `logger = logging.getLogger(__name__)` e usa-o em vez de ser silencioso. O `_on_result` em `main.py` ganha uma segunda linha de resumo calculada a partir dos campos do registro já retornado.

**Tech Stack:** Python stdlib `logging`, `pytest` `caplog` fixture (já disponível via `pytest`)

---

## Mapa de arquivos

| Arquivo | Mudança |
|---|---|
| `.gitignore` | Adicionar `data/logs/` |
| `scripts/main.py` | `_configurar_logging()`, `_CAMPOS_DISPLAY`, `_resumo_campos()`, enriquecer `_on_result` |
| `catalog/metadata/api.py` | `logger`, instrumentar `_get_json()` e `buscar_metadados()` |
| `catalog/metadata/worker.py` | `logger`, instrumentar loop principal |
| `catalog/storage/persistence.py` | `logger`, instrumentar `salvar()` e `reescrever_registros()` |
| `catalog/storage/git_sync.py` | `logger`, instrumentar funções públicas |
| `catalog/storage/github_sync.py` | `logger`, instrumentar funções públicas |
| `tests/test_main_helpers.py` | Novo — testa `_resumo_campos()` |
| `tests/test_api.py` | Adicionar testes de logging com `caplog` |
| `tests/test_worker.py` | Adicionar teste de logging com `caplog` |
| `tests/test_persistence.py` | Adicionar testes de logging com `caplog` |
| `tests/test_git_sync.py` | Adicionar teste de logging com `caplog` |

---

## Task 1: Infraestrutura — `.gitignore` e `_configurar_logging()`

**Files:**
- Modify: `.gitignore`
- Modify: `scripts/main.py`

- [ ] **Step 1: Adicionar `data/logs/` ao `.gitignore`**

Adicionar ao final de `.gitignore`:

```
data/logs/
```

- [ ] **Step 2: Adicionar imports e constante `LOG_DIR` em `scripts/main.py`**

No bloco de imports do topo de `scripts/main.py`, adicionar:

```python
import logging
from datetime import date
```

Após os imports existentes (antes da primeira `def`), adicionar:

```python
LOG_DIR = Path("data/logs")
```

- [ ] **Step 3: Adicionar `_configurar_logging()` em `scripts/main.py`**

Adicionar a função antes de `_make_on_result`:

```python
def _configurar_logging() -> None:
    LOG_DIR.mkdir(parents=True, exist_ok=True)
    raiz = logging.getLogger("catalog")
    raiz.setLevel(logging.DEBUG)

    sh = logging.StreamHandler()
    sh.setLevel(logging.WARNING)
    sh.setFormatter(logging.Formatter("[%(levelname)s] %(message)s"))

    fh = logging.FileHandler(LOG_DIR / f"{date.today()}.log", encoding="utf-8")
    fh.setLevel(logging.DEBUG)
    fh.setFormatter(logging.Formatter(
        "[%(asctime)s] [%(levelname)-7s] %(name)s — %(message)s",
        datefmt="%H:%M:%S",
    ))

    raiz.addHandler(sh)
    raiz.addHandler(fh)
```

- [ ] **Step 4: Chamar `_configurar_logging()` no início de `main()`**

Na função `main()`, antes do primeiro `print(...)`, adicionar:

```python
def main() -> None:
    _configurar_logging()
    print("📚  Cadastro de biblioteca — modo assíncrono")
    # ... resto igual
```

- [ ] **Step 5: Executar os testes para garantir que nada quebrou**

```bash
pytest -x -q
```

Saída esperada: todos os testes passando (0 falhas).

- [ ] **Step 6: Commit**

```bash
git add .gitignore scripts/main.py
git commit -m "feat: infraestrutura de logging — _configurar_logging() e data/logs/"
```

---

## Task 2: Enriquecer `_on_result` com resumo compacto

**Files:**
- Modify: `scripts/main.py`
- Create: `tests/test_main_helpers.py`

- [ ] **Step 1: Escrever o teste que falha para `_resumo_campos`**

Criar `tests/test_main_helpers.py`:

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "scripts"))
import main as main_module


def _registro_completo():
    return {
        "isbn": "9781098115784",
        "titulo": "Machine Learning Design Patterns",
        "autores": "Sara Robinson",
        "editora": "O'Reilly",
        "ano": "2020",
        "paginas": "400",
        "idioma": "en",
        "assuntos": "Computers",
        "capa_url": "https://example.com/capa.jpg",
        "capa_fonte": "openlibrary_isbn",
        "fonte": "openlibrary",
        "data_cadastro": "2026-06-21T10:00:00",
        "estante": "",
        "prateleira": "",
        "etiquetas": "",
    }


def test_resumo_campos_retorna_campos_presentes():
    resultado = main_module._resumo_campos(_registro_completo())
    assert "título" in resultado
    assert "autores" in resultado
    assert "páginas" in resultado
    assert "idioma" in resultado


def test_resumo_campos_exclui_campos_de_sistema():
    resultado = main_module._resumo_campos(_registro_completo())
    assert "isbn" not in resultado
    assert "fonte" not in resultado
    assert "data_cadastro" not in resultado
    assert "capa_url" not in resultado
    assert "estante" not in resultado


def test_resumo_campos_sem_nada_retorna_nenhum():
    vazio = {k: "" for k in _registro_completo()}
    resultado = main_module._resumo_campos(vazio)
    assert resultado == "nenhum"


def test_resumo_campos_com_etiquetas():
    r = {**_registro_completo(), "etiquetas": "doutorado, lazer"}
    resultado = main_module._resumo_campos(r)
    assert "etiquetas" in resultado
```

- [ ] **Step 2: Executar o teste para confirmar que falha**

```bash
pytest tests/test_main_helpers.py -v
```

Saída esperada: `AttributeError: module 'main' has no attribute '_resumo_campos'`

- [ ] **Step 3: Implementar `_CAMPOS_DISPLAY` e `_resumo_campos` em `scripts/main.py`**

Adicionar logo antes de `_make_on_result` em `scripts/main.py`:

```python
_CAMPOS_DISPLAY = {
    "titulo":    "título",
    "autores":   "autores",
    "editora":   "editora",
    "ano":       "ano",
    "paginas":   "páginas",
    "idioma":    "idioma",
    "assuntos":  "assuntos",
    "etiquetas": "etiquetas",
}


def _resumo_campos(registro: dict) -> str:
    recuperados = [
        label
        for campo, label in _CAMPOS_DISPLAY.items()
        if str(registro.get(campo, "")).strip()
    ]
    return ", ".join(recuperados) if recuperados else "nenhum"
```

- [ ] **Step 4: Executar o teste para confirmar que passa**

```bash
pytest tests/test_main_helpers.py -v
```

Saída esperada: 4 testes PASSED.

- [ ] **Step 5: Atualizar `_on_result` para exibir o resumo compacto**

Substituir o corpo de `_on_result` dentro de `_make_on_result`:

```python
def _make_on_result(fila: "queue.Queue"):
    def _on_result(registro: dict) -> None:
        if "_erro" in registro:
            print(f"\n  ✗ [{registro['isbn']}] erro inesperado: {registro['_erro']}  (fica pendente)",
                  flush=True)
        elif registro.get("titulo"):
            print(f"\n  ✓ [{registro['isbn']}] {registro['titulo']} — {registro['autores']}",
                  flush=True)
            fonte = registro.get("fonte", "")
            capa = registro.get("capa_fonte") or "sem capa"
            campos = _resumo_campos(registro)
            print(f"     → fonte: {fonte} | capa: {capa} | campos: {campos}", flush=True)
        else:
            print(f"\n  ⚠  [{registro['isbn']}] sem metadados — salvo só o ISBN",
                  flush=True)
            print(f"     → nenhuma fonte retornou dados", flush=True)

        restante = fila.unfinished_tasks - 1
        if restante > 0:
            print(f"     ({restante} na fila)", flush=True)
        else:
            print(f"     (fila vazia ✓)", flush=True)

    return _on_result
```

- [ ] **Step 6: Executar todos os testes**

```bash
pytest -x -q
```

Saída esperada: todos passando.

- [ ] **Step 7: Commit**

```bash
git add scripts/main.py tests/test_main_helpers.py
git commit -m "feat: resumo compacto no on_result — fonte, capa, campos recuperados"
```

---

## Task 3: Instrumentar `catalog/metadata/api.py`

**Files:**
- Modify: `catalog/metadata/api.py`
- Modify: `tests/test_api.py`

- [ ] **Step 1: Escrever os testes de logging que vão falhar**

Adicionar ao final de `tests/test_api.py`:

```python
import logging


def test_buscar_metadados_loga_info_fonte_encontrada(mocker, caplog):
    payload = {
        "titulo": "Machine Learning Design Patterns",
        "autores": "Sara Robinson",
        "editora": "O'Reilly",
        "ano": "2020",
        "paginas": 400,
        "idioma": "en",
        "assuntos": "Computers",
        "capa_url": "",
        "fonte": "openlibrary",
    }
    mocker.patch("catalog.metadata.api.buscar_brasil_api", return_value=None)
    mocker.patch("catalog.metadata.api.buscar_open_library", return_value=payload)
    mocker.patch("catalog.metadata.api.buscar_google_books", return_value=None)
    mocker.patch("catalog.metadata.api.buscar_isbndb", return_value=None)
    mocker.patch("catalog.metadata.api.buscar_open_library_edicao", return_value=None)

    with caplog.at_level(logging.INFO, logger="catalog.metadata.api"):
        buscar_metadados(ISBN)

    assert any("metadados obtidos via openlibrary" in r.message for r in caplog.records)


def test_buscar_metadados_loga_warning_nao_encontrado(mocker, caplog):
    for fn in ["buscar_brasil_api", "buscar_open_library", "buscar_google_books",
               "buscar_isbndb", "buscar_open_library_edicao"]:
        mocker.patch(f"catalog.metadata.api.{fn}", return_value=None)

    with caplog.at_level(logging.WARNING, logger="catalog.metadata.api"):
        buscar_metadados(ISBN)

    assert any("nenhuma fonte retornou metadados" in r.message for r in caplog.records)
```

- [ ] **Step 2: Executar para confirmar que falham**

```bash
pytest tests/test_api.py::test_buscar_metadados_loga_info_fonte_encontrada tests/test_api.py::test_buscar_metadados_loga_warning_nao_encontrado -v
```

Saída esperada: 2 FAILED (AssertionError — sem registros de log).

- [ ] **Step 3: Adicionar `logger` no topo de `catalog/metadata/api.py`**

Após os imports existentes, antes de `FONTES_METADADOS`:

```python
import logging

logger = logging.getLogger(__name__)
```

- [ ] **Step 4: Instrumentar `_get_json()` em `catalog/metadata/api.py`**

Substituir o corpo de `_get_json()`:

```python
def _get_json(
    url: str,
    tentativas: int = 3,
    timeout: int = 20,
    headers: dict | None = None,
) -> dict | None:
    """GET com retry em timeouts e 429. Honra Retry-After quando presente."""
    espera = 2
    for tentativa in range(1, tentativas + 1):
        logger.debug("GET %s (tentativa %d/%d)", url, tentativa, tentativas)
        try:
            r = requests.get(url, timeout=timeout, headers=headers or {})
            if r.status_code == 429:
                pausa = int(r.headers.get("Retry-After", espera))
                logger.warning("Rate limit 429 em %s — aguardando %ds", url, pausa)
                time.sleep(pausa)
                espera = min(espera * 2, 30)
                continue
            r.raise_for_status()
            return r.json()
        except (requests.Timeout, requests.ConnectionError):
            if tentativa == tentativas:
                logger.error("Falha após %d tentativas: %s", tentativas, url)
                raise
            time.sleep(espera)
            espera = min(espera * 2, 30)
    return None
```

- [ ] **Step 5: Instrumentar `buscar_metadados()` em `catalog/metadata/api.py`**

Substituir o corpo completo de `buscar_metadados()`:

```python
def buscar_metadados(isbn: str, apis_metadados: list[str] | None = None) -> dict:
    """Cascata de APIs. ISBNs brasileiros (978-85/978-65) consultam BrasilAPI primeiro.

    Quando a fonte principal encontra título mas não o ano, percorre as fontes
    restantes para suplementar apenas o campo ano, sem substituir os demais dados.

    apis_metadados: lista de IDs de fontes a usar (None = lê de config_apis.json).
    """
    _MAP = {
        "brasilapi":          buscar_brasil_api,
        "openlibrary":        buscar_open_library,
        "googlebooks":        buscar_google_books,
        "isbndb":             buscar_isbndb,
        "openlibrary_edicao": buscar_open_library_edicao,
    }
    habilitadas = set(apis_metadados or carregar_config_apis()["metadados"])

    e_brasileiro = isbn.startswith("97885") or isbn.startswith("97865")
    ordem_br  = ["brasilapi", "openlibrary", "googlebooks", "isbndb", "openlibrary_edicao"]
    ordem_int = ["openlibrary", "googlebooks", "brasilapi", "isbndb", "openlibrary_edicao"]
    ordem = ordem_br if e_brasileiro else ordem_int
    fontes = [(k, _MAP[k]) for k in ordem if k in habilitadas and k in _MAP]

    dados = None
    for nome_fonte, buscar in fontes:
        logger.debug("[%s] tentando %s", isbn, nome_fonte)
        resultado = buscar(isbn)
        if not resultado or not resultado.get("titulo"):
            logger.debug("[%s] %s → não encontrado", isbn, nome_fonte)
            continue
        if dados is None:
            logger.debug("[%s] %s → encontrado (título: %s)", isbn, nome_fonte, resultado.get("titulo", ""))
            dados = resultado
            if (dados.get("ano") or "").strip():
                break
        elif not (dados.get("ano") or "").strip() and (resultado.get("ano") or "").strip():
            logger.debug("[%s] ano suplementado via %s", isbn, nome_fonte)
            dados["ano"] = resultado["ano"].strip()
            break

    if not dados:
        logger.warning("[%s] nenhuma fonte retornou metadados", isbn)
        dados = {k: "" for k in CSV_HEADERS}
        dados["fonte"] = "nao_encontrado"
    else:
        logger.info("[%s] metadados obtidos via %s", isbn, dados["fonte"])
    dados["isbn"] = isbn
    dados["data_cadastro"] = datetime.now().isoformat(timespec="seconds")
    return dados
```

- [ ] **Step 6: Executar os testes novos para confirmar que passam**

```bash
pytest tests/test_api.py::test_buscar_metadados_loga_info_fonte_encontrada tests/test_api.py::test_buscar_metadados_loga_warning_nao_encontrado -v
```

Saída esperada: 2 PASSED.

- [ ] **Step 7: Executar toda a suite**

```bash
pytest -x -q
```

Saída esperada: todos passando.

- [ ] **Step 8: Commit**

```bash
git add catalog/metadata/api.py tests/test_api.py
git commit -m "feat: logging em catalog.metadata.api — _get_json, buscar_metadados"
```

---

## Task 4: Instrumentar `catalog/metadata/worker.py`

**Files:**
- Modify: `catalog/metadata/worker.py`
- Modify: `tests/test_worker.py`

- [ ] **Step 1: Escrever o teste de logging que vai falhar**

Adicionar ao final de `tests/test_worker.py`:

```python
import logging


def test_worker_loga_error_em_excecao(mocker, caplog, sample_isbn):
    mocker.patch.object(
        _worker_mod, "buscar_metadados",
        side_effect=RuntimeError("falha de rede"),
    )
    mocker.patch.object(_worker_mod, "salvar")
    mocker.patch.object(_worker_mod, "remover_pendente")

    fila = queue.Queue()
    fila.put(sample_isbn)

    with caplog.at_level(logging.ERROR, logger="catalog.metadata.worker"):
        _run_worker(fila)

    mensagens = [r.message for r in caplog.records]
    assert any("erro inesperado" in m for m in mensagens)
    assert any(sample_isbn in m for m in mensagens)
```

- [ ] **Step 2: Executar para confirmar que falha**

```bash
pytest tests/test_worker.py::test_worker_loga_error_em_excecao -v
```

Saída esperada: FAILED (AssertionError — sem registros de log).

- [ ] **Step 3: Instrumentar `catalog/metadata/worker.py`**

Substituir o conteúdo completo do arquivo:

```python
import logging
import queue
import threading
from collections.abc import Callable

from .api import buscar_capa, buscar_metadados
from ..storage.persistence import remover_pendente, salvar

logger = logging.getLogger(__name__)


def worker(
    fila: queue.Queue,
    parar_evento: threading.Event,
    on_result: Callable[[dict], None] | None = None,
) -> None:
    """Consome ISBNs da fila e busca os metadados em background.

    on_result é chamado com o registro salvo (sucesso) ou com
    {"isbn": ..., "_erro": ...} em caso de exceção inesperada.
    """
    while not parar_evento.is_set():
        try:
            isbn = fila.get(timeout=0.5)
        except queue.Empty:
            continue
        if isbn is None:
            fila.task_done()
            break

        logger.debug("[%s] recebido da fila", isbn)
        try:
            registro = buscar_metadados(isbn)
            metadata_capa = registro.get("capa_url", "")
            capa_dedicada, capa_fonte = buscar_capa(
                isbn, registro.get("titulo", ""), registro.get("autores", "")
            )
            if capa_dedicada:
                registro["capa_url"] = capa_dedicada
                registro["capa_fonte"] = capa_fonte
            else:
                registro["capa_url"] = metadata_capa
                registro["capa_fonte"] = ""
            salvar(registro)
            remover_pendente(isbn)
            titulo = registro.get("titulo") or "sem metadados"
            logger.info("[%s] processado — %s", isbn, titulo)
            if on_result is not None:
                on_result(registro)
        except Exception as e:
            logger.error("[%s] erro inesperado: %s", isbn, e)
            if on_result is not None:
                on_result({"isbn": isbn, "_erro": str(e)})
        finally:
            fila.task_done()
```

- [ ] **Step 4: Executar o novo teste**

```bash
pytest tests/test_worker.py::test_worker_loga_error_em_excecao -v
```

Saída esperada: PASSED.

- [ ] **Step 5: Executar toda a suite**

```bash
pytest -x -q
```

Saída esperada: todos passando.

- [ ] **Step 6: Commit**

```bash
git add catalog/metadata/worker.py tests/test_worker.py
git commit -m "feat: logging em catalog.metadata.worker"
```

---

## Task 5: Instrumentar `catalog/storage/persistence.py`

**Files:**
- Modify: `catalog/storage/persistence.py`
- Modify: `tests/test_persistence.py`

- [ ] **Step 1: Escrever os testes de logging que vão falhar**

Adicionar ao final de `tests/test_persistence.py`:

```python
import logging


def test_salvar_loga_debug(sample_record, tmp_data_dir, caplog):
    with caplog.at_level(logging.DEBUG, logger="catalog.storage.persistence"):
        salvar(sample_record)
    mensagens = [r.message for r in caplog.records]
    assert any("salvando" in m for m in mensagens)
    assert any(sample_record["isbn"] in m for m in mensagens)


def test_reescrever_registros_loga_debug(sample_record, tmp_data_dir, caplog):
    salvar(sample_record)
    registros = carregar_todos_registros()
    with caplog.at_level(logging.DEBUG, logger="catalog.storage.persistence"):
        reescrever_registros(registros)
    assert any("reescrevendo" in r.message for r in caplog.records)
```

- [ ] **Step 2: Executar para confirmar que falham**

```bash
pytest tests/test_persistence.py::test_salvar_loga_debug tests/test_persistence.py::test_reescrever_registros_loga_debug -v
```

Saída esperada: 2 FAILED.

- [ ] **Step 3: Adicionar `logger` e instrumentar `catalog/storage/persistence.py`**

Adicionar `import logging` e `logger` após o bloco de imports existente:

```python
import logging

logger = logging.getLogger(__name__)
```

Substituir o corpo de `salvar()`:

```python
def salvar(registro: dict) -> None:
    from ..series import compor_titulo, detectar_serie  # import local evita ciclo

    titulo = registro.get("titulo", "")
    isbn = registro.get("isbn", "")
    if titulo:
        detectado = detectar_serie(titulo)
        if detectado:
            registro = {**registro, "titulo": compor_titulo(**detectado)}

    logger.debug("salvando %s (%s)", isbn, titulo or "sem título")
    with _io_lock:
        novo = not Path(CSV_FILE).exists()
        with open(CSV_FILE, "a", newline="", encoding="utf-8") as f:
            w = csv.DictWriter(f, fieldnames=CSV_HEADERS)
            if novo:
                w.writeheader()
            w.writerow({k: registro.get(k, "") for k in CSV_HEADERS})
        with open(JSON_FILE, "a", encoding="utf-8") as f:
            f.write(json.dumps(registro, ensure_ascii=False) + "\n")
```

Substituir o corpo de `reescrever_registros()`:

```python
def reescrever_registros(registros: list[dict]) -> None:
    logger.debug("reescrevendo %d registros", len(registros))
    with _io_lock:
        with open(JSON_FILE, "w", encoding="utf-8") as f:
            for r in registros:
                f.write(json.dumps(r, ensure_ascii=False) + "\n")
        with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
            w = csv.DictWriter(f, fieldnames=CSV_HEADERS)
            w.writeheader()
            for r in registros:
                w.writerow({k: r.get(k, "") for k in CSV_HEADERS})
    if len(registros) == 1:
        titulo = registros[0].get("titulo") or registros[0].get("isbn", "?")
    else:
        titulo = f"{len(registros)} registros"
    git_sync.commit_se_houver_mudancas(f"edit: {titulo}", arquivos=[JSON_FILE, CSV_FILE])
```

- [ ] **Step 4: Executar os novos testes**

```bash
pytest tests/test_persistence.py::test_salvar_loga_debug tests/test_persistence.py::test_reescrever_registros_loga_debug -v
```

Saída esperada: 2 PASSED.

- [ ] **Step 5: Executar toda a suite**

```bash
pytest -x -q
```

Saída esperada: todos passando.

- [ ] **Step 6: Commit**

```bash
git add catalog/storage/persistence.py tests/test_persistence.py
git commit -m "feat: logging em catalog.storage.persistence"
```

---

## Task 6: Instrumentar `catalog/storage/git_sync.py` e `github_sync.py`

**Files:**
- Modify: `catalog/storage/git_sync.py`
- Modify: `catalog/storage/github_sync.py`
- Modify: `tests/test_git_sync.py`

- [ ] **Step 1: Escrever o teste de logging que vai falhar**

Adicionar ao final de `tests/test_git_sync.py`:

```python
import logging


class TestLogging:
    def test_commit_loga_info_mensagem(self, caplog):
        with patch.object(git_sync, "_git"), \
             patch.object(git_sync, "_tem_mudancas_staged", return_value=True), \
             caplog.at_level(logging.INFO, logger="catalog.storage.git_sync"):
            git_sync.commit_se_houver_mudancas("edit: Livro Teste")
        assert any("commit: edit: Livro Teste" in r.message for r in caplog.records)

    def test_sem_mudancas_loga_debug(self, caplog):
        with patch.object(git_sync, "_git"), \
             patch.object(git_sync, "_tem_mudancas_staged", return_value=False), \
             caplog.at_level(logging.DEBUG, logger="catalog.storage.git_sync"):
            git_sync.commit_se_houver_mudancas("edit: Livro Teste")
        assert any("nenhuma mudança" in r.message for r in caplog.records)
```

- [ ] **Step 2: Executar para confirmar que falham**

```bash
pytest tests/test_git_sync.py::TestLogging -v
```

Saída esperada: 2 FAILED.

- [ ] **Step 3: Instrumentar `catalog/storage/git_sync.py`**

Adicionar após o bloco de imports (antes da primeira `def`):

```python
import logging

logger = logging.getLogger(__name__)
```

Substituir `garantir_branch_sessao()`:

```python
def garantir_branch_sessao() -> str:
    from catalog.storage import github_sync
    if github_sync.disponivel():
        return github_sync.garantir_branch_sessao()
    # git CLI local
    branch = branch_atual()
    if branch.startswith("data/"):
        logger.info("branch de sessão: %s", branch)
        return branch
    nome = f"data/{date.today().isoformat()}"
    try:
        _git("checkout", "-b", nome)
    except subprocess.CalledProcessError:
        try:
            _git("checkout", nome)
        except (subprocess.CalledProcessError, FileNotFoundError) as e:
            raise RuntimeError(f"Não foi possível criar/acessar o branch {nome}: {e}") from e
    logger.info("branch de sessão: %s", nome)
    return nome
```

Substituir `commit_se_houver_mudancas()`:

```python
def commit_se_houver_mudancas(mensagem: str, arquivos=None) -> bool:
    from catalog.storage import github_sync
    if github_sync.disponivel():
        if not arquivos:
            return False
        return github_sync.commit_arquivos(list(arquivos), mensagem)
    # git CLI local
    try:
        _git("add", "data/")
        if not _tem_mudancas_staged():
            logger.debug("nenhuma mudança para commitar")
            return False
        _git("commit", "-m", mensagem)
        logger.info("commit: %s", mensagem)
        return True
    except (subprocess.CalledProcessError, FileNotFoundError) as e:
        logger.error("falha no sync: %s", e)
        return False
```

Substituir `finalizar_sessao()` — adicionar `logger.info` após a PR ser aberta:

```python
def finalizar_sessao() -> str:
    from catalog.storage import github_sync
    if github_sync.disponivel():
        return github_sync.finalizar_sessao()
    # git CLI local
    n = contar_commits_sessao()
    if n == 0:
        raise ValueError("Nenhuma alteração para enviar.")
    hoje = date.today().isoformat()
    sufixo = "ões" if n > 1 else "ão"
    mensagem = f"data: sessão {hoje} – {n} alteraç{sufixo}"
    branch = branch_atual()
    try:
        _git("reset", "--soft", "main")
        _git("commit", "-m", mensagem)
        _git("push", "-u", "origin", branch)
    except (subprocess.CalledProcessError, FileNotFoundError) as e:
        raise RuntimeError(f"Erro ao finalizar sessão via git CLI: {e}") from e
    url = _gh(
        "pr", "create",
        "--title", mensagem,
        "--body", f"Sessão de {hoje}: {n} alteraç{sufixo} nos dados da biblioteca.",
        "--head", branch,
        "--base", "main",
    )
    logger.info("PR aberta: %s", url)
    return url
```

- [ ] **Step 4: Instrumentar `catalog/storage/github_sync.py`**

Adicionar `import logging` no bloco de imports e `logger = logging.getLogger(__name__)` após os imports existentes, antes da primeira `def`.

**Em `garantir_branch_sessao()`**, substituir as duas linhas de `return` com dados de branch pelos trechos abaixo:

```python
# Caso 1: branch já existia no GitHub (linha ~95)
    if _get_branch_sha(nome):
        _sessao_branch = nome
        logger.info("branch de sessão: %s", nome)
        return nome

# Caso 2: branch recém-criado (linha ~122)
    _sessao_branch = nome
    logger.info("branch de sessão: %s", nome)
    return nome
```

**Em `commit_arquivos()`**, substituir o `return rr.status_code == 200` final pelo trecho:

```python
    if rr.status_code == 200:
        logger.info("commit: %s", mensagem)
        return True
    logger.warning("falha ao atualizar ref do branch: HTTP %s", rr.status_code)
    return False
```

**Em `finalizar_sessao()`**, substituir as duas linhas de `return` de sucesso:

```python
# Caso 1: PR já existia (linha ~236)
    if existing.status_code == 200 and existing.json():
        url = existing.json()[0]["html_url"]
        logger.info("PR aberta: %s", url)
        return url

# Caso 2: PR recém-criada (linha ~244)
    if r.status_code == 201:
        url = r.json()["html_url"]
        logger.info("PR aberta: %s", url)
        return url
    logger.error("falha ao criar PR: HTTP %s — %s", r.status_code, r.json().get("message", ""))
    raise RuntimeError(
        f"Erro ao criar PR: HTTP {r.status_code} — {r.json().get('message', '')}"
    )
```

- [ ] **Step 5: Executar os novos testes**

```bash
pytest tests/test_git_sync.py::TestLogging -v
```

Saída esperada: 2 PASSED.

- [ ] **Step 6: Executar toda a suite**

```bash
pytest -x -q
```

Saída esperada: todos passando.

- [ ] **Step 7: Commit final**

```bash
git add catalog/storage/git_sync.py catalog/storage/github_sync.py tests/test_git_sync.py
git commit -m "feat: logging em catalog.storage.git_sync e github_sync"
```

---

## Verificação final

Após todos os tasks, verificar que o comportamento no terminal está correto:

```bash
python scripts/main.py
# Escanear um ISBN
# Confirmar que aparece:
#   ✓ [ISBN] Título — Autor
#      → fonte: openlibrary | capa: openlibrary_isbn | campos: título, autores, editora, ano, páginas, idioma
# Confirmar que o arquivo data/logs/YYYY-MM-DD.log foi criado com as entradas DEBUG
```
